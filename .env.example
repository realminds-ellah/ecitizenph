# Public key for the eVerify Face Liveness Web SDK (hackathon sandbox).
# Safe to expose client-side — it's the public half of the credential pair.
# Leave unset to fall back to the local simulated liveness animation.
VITE_EVERIFY_PUBLIC_KEY=
